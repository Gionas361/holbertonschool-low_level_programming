#include "main.h"
#include <stdio.h>

/**
 * add - prints out the last digit of the number string provided
 *
 * @n: The string provided by the user
 * @c: The string provided by the user, the seocnd comming
 *
 * Return: gives out the result of calculation.
 */

int add(int n, int c)
{
	int f;
	f = (n + c);

	return (f);
}
