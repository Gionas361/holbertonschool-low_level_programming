#include <stdio.h>

int main(void)
{
/*puts will print the text inside of the quotation*/
puts("\"Programming is like building a multilingual puzzle");
/*return will wake the value of void = 0*/
return (0);
}
