#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies the 2 strings provided by the user
 *
 * @a: The first string provided by the user
 * @b: The second string provided by the user
 *
 * Return: gives out the result of calculation.
 */

int mul(int a, int b)
{
	int r;

	r = (a * b);
    return (r);
}
