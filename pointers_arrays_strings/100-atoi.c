#include "main.h"
#include <stdio.h>

/**
 * _atoi - prints a triangle
 *
 * @s: the string provided by the user
 *
 * Return: gives out the result of calculation.
 */

int _atoi(char *s)
{
    int stringLenght;
	int numCheck;

    for (i = 0 ; s[i] ; i++)
    {
    }

    for ()

	return (int baby);
}
